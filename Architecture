User → Load Balancer → EC2 (ASG)
                         ├── Docker (Frontend - React/NGINX)
                         ├── Docker (Backend - Node.js API)
                         ├── Docker (Prometheus)
                         └── Docker (Grafana)
                         ↓
                      RDS (MySQL)
